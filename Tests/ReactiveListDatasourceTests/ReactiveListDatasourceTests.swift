import XCTest
@testable import ReactiveListDatasource

final class ReactiveListDatasourceTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ReactiveListDatasource().text, "Hello, World!")
    }
}
